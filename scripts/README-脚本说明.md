# 脚本使用说明

## 📋 脚本概览

本目录包含了 nRF52810-AirTag-Toolkit 项目的所有自动化脚本，涵盖环境检查、编译刷写、设备管理、项目备份等功能。

## 🚀 快速开始脚本

### 环境相关
- **`one_click_verify.sh`** - 一键环境验证 ⭐️
- **`one_click_install.sh`** - 一键环境安装
- **`setup_nrf52810.sh`** - nRF52810 环境检查

### 编译刷写
- **`compile_and_flash_2s.sh`** - 一键编译刷写 (2秒广播) ⭐️
- **`compile_and_flash_device.sh`** - 设备编译刷写

## 🛠️ 设备管理脚本

### 密钥管理
- **`generate_device_keys.sh`** - 生成设备密钥
- **`list_device_keys.sh`** - 列出设备密钥
- **`setup_device.sh`** - 设备初始化设置

### 芯片恢复 ⭐️ 新增
- **`recover_protected_chip.sh`** - 完整芯片保护恢复
- **`quick_chip_recovery.sh`** - 快速芯片恢复

## 📦 项目管理脚本

### 备份部署
- **`project_backup.sh`** - 项目备份
- **`final_package.sh`** - 最终打包

## 📖 详细使用说明

### 🔧 环境检查和安装

#### 检查开发环境
```bash
# 全面环境检查 (推荐)
./scripts/one_click_verify.sh

# nRF52810 专用检查
./scripts/setup_nrf52810.sh
```

#### 安装开发环境
```bash
# 一键安装所有必需工具
./scripts/one_click_install.sh
```

### 🔥 编译和刷写

#### 快速编译刷写
```bash
# 使用默认配置 (2秒广播间隔)
./scripts/compile_and_flash_2s.sh

# 指定设备编译刷写
./scripts/compile_and_flash_device.sh [设备名]
```

### 🔑 密钥管理

#### 生成新设备密钥
```bash
# 生成指定数量的密钥
./scripts/generate_device_keys.sh [设备名] [密钥数量]

# 示例: 为 MyDevice 生成 200 个密钥
./scripts/generate_device_keys.sh MyDevice 200
```

#### 查看现有密钥
```bash
# 列出所有设备的密钥信息
./scripts/list_device_keys.sh

# 查看特定设备的密钥
./scripts/list_device_keys.sh [设备名]
```

### 🆘 芯片保护恢复 (重要)

#### 快速恢复 (推荐)
```bash
# 自动检测并恢复 nRF52 芯片
./scripts/quick_chip_recovery.sh

# 恢复 nRF51 芯片
./scripts/quick_chip_recovery.sh nrf51
```

#### 完整恢复选项
```bash
# 自动检测恢复
./scripts/recover_protected_chip.sh

# 指定芯片类型
./scripts/recover_protected_chip.sh -t nrf52810

# 指定调试器类型
./scripts/recover_protected_chip.sh -d jlink

# 使用自定义配置
./scripts/recover_protected_chip.sh -c custom.cfg

# 查看所有选项
./scripts/recover_protected_chip.sh -h
```

### 📦 项目备份

#### 创建项目备份
```bash
# 完整项目备份
./scripts/project_backup.sh

# 创建最终发布包
./scripts/final_package.sh
```

## 🔍 脚本功能对照表

| 脚本名称 | 主要功能 | 使用场景 | 执行时间 |
|---------|---------|---------|---------|
| `one_click_verify.sh` | 环境验证 | 检查开发环境完整性 | 1-2分钟 |
| `one_click_install.sh` | 环境安装 | 首次环境搭建 | 10-30分钟 |
| `compile_and_flash_2s.sh` | 编译刷写 | 日常开发刷写 | 30-60秒 |
| `quick_chip_recovery.sh` | 芯片恢复 | 芯片保护问题 | 30秒-2分钟 |
| `generate_device_keys.sh` | 密钥生成 | 新设备配置 | 10-30秒 |
| `project_backup.sh` | 项目备份 | 项目保存 | 1-5分钟 |

## 🚨 常见使用场景

### 场景1: 首次环境搭建
```bash
1. ./scripts/one_click_install.sh      # 安装环境
2. ./scripts/one_click_verify.sh       # 验证安装
3. ./scripts/generate_device_keys.sh MyDevice 200  # 生成密钥
4. ./scripts/compile_and_flash_2s.sh   # 编译刷写
```

### 场景2: 日常开发
```bash
1. ./scripts/setup_nrf52810.sh         # 检查环境
2. ./scripts/compile_and_flash_2s.sh   # 编译刷写
```

### 场景3: 芯片保护问题
```bash
1. ./scripts/quick_chip_recovery.sh    # 快速恢复
2. ./scripts/compile_and_flash_2s.sh   # 重新刷写
```

### 场景4: 批量设备管理
```bash
1. ./scripts/generate_device_keys.sh Device1 200
2. ./scripts/generate_device_keys.sh Device2 200
3. ./scripts/list_device_keys.sh       # 查看所有密钥
4. ./scripts/compile_and_flash_device.sh Device1
```

### 场景5: 项目交付
```bash
1. ./scripts/project_backup.sh         # 备份项目
2. ./scripts/final_package.sh          # 创建发布包
```

## ⚠️ 注意事项

### 权限要求
- 所有脚本需要执行权限: `chmod +x scripts/*.sh`
- 某些操作可能需要管理员权限

### 硬件要求
- 编译刷写脚本需要连接硬件
- 芯片恢复脚本需要调试器连接

### 环境依赖
- macOS 系统 (主要支持)
- 已安装必需的开发工具
- 正确配置的项目结构

## 🔧 自定义和扩展

### 修改默认参数
编辑脚本中的配置变量:
```bash
# 在 compile_and_flash_2s.sh 中
ADVERTISING_INTERVAL=2000  # 修改广播间隔
MAX_KEYS=200              # 修改最大密钥数
```

### 添加新脚本
1. 创建新脚本文件
2. 添加执行权限: `chmod +x new_script.sh`
3. 更新此说明文档

## 📞 获取帮助

### 脚本问题
- 查看脚本内的帮助信息: `script_name.sh -h`
- 检查日志文件: `*.log`
- 运行环境验证: `./scripts/one_click_verify.sh`

### 文档参考
- [芯片保护恢复指南](../docs/10-芯片保护恢复指南.md)
- [快速参考手册](../docs/05-快速参考手册.md)
- [硬件连接与刷写指南](../docs/04-硬件连接与刷写.md)

---
*最后更新: 2024年7月*
# 🌐 Web Studio 完全指南

Web Studio 是本项目的核心工具，提供了业界最优雅的刷机体验。

## 核心功能

### 1. 自动检测

- **调试器识别**: 自动检测 J-Link / ST-Link / DAPLink
- **芯片识别**: 读取 FICR 寄存器，自动识别 nRF51822/52810/52832/52811
- **自动纠错**: 即使手动选错芯片型号，系统也会在刷写时自动纠正

### 2. 密钥模式

#### Dynamic 模式（推荐）

- **原理**: 注入 32 字节随机种子到芯片
- **优势**: 无限动态密钥，追踪效果最佳
- **生成文件**:
  - `seed_XXX.hex/bin`: 种子文件
  - `XXX_devices.json`: 离线密钥配置（200个）

#### Static 模式

- **原理**: 注入 200 个固定公钥
- **适用**: 兼容旧版 OpenHaystack
- **生成文件**:
  - `XXX_keyfile`: 密钥二进制文件
  - `XXX_devices.json`: 设备配置文件

### 3. 参数配置

#### 设备名称

- **格式**: 前缀 + 数字，总长度 6 字符
- **示例**: `MSF001`, `TAG099`
- **自动补零**: 输入 `MSF1` 会自动变为 `MSF001`

#### 广播间隔

```
基础间隔 + (设备编号 × 间隔步长)
```

用于多设备批量刷写时避免干扰。

#### DCDC 选项

- 仅支持 DCDC 的硬件才能启用（Web Studio 会根据检测结果给出建议）
- nRF52 系列大多支持，nRF51 系列通常不支持
- 若不确定，建议禁用（使用 LDO 更兼容）

---

## 界面功能详解

### 左侧边栏

#### 芯片选择

- 默认显示"Auto Detect"
- 系统会自动读取芯片型号并更新显示

#### 调试器选择

- **J-Link**: nrfjprog 优先，失败时回退到 JLinkExe
- **ST-Link**: 使用 OpenOCD
- **DAPLink**: 使用 OpenOCD (CMSIS-DAP 协议)

#### 模式切换

- **Dynamic**: 绿色按钮高亮
- **Static**: 灰色按钮高亮

### 主操作区

#### START FLASHING 按钮

点击后依次执行：

1. 硬件预检（Probing Hardware Link）
2. 生成种子/密钥
3. 编译固件
4. 注入配置（Patching Binary）
5. 刷写设备

#### 诊断日志

实时显示操作过程：

- **绿色**: 成功信息
- **黄色**: 警告
- **红色**: 错误
- **紫色**: 关键步骤（如种子生成）

### 状态覆盖层

刷写过程中会显示全屏状态覆盖：

- **Waiting for Device Connection...**: 等待连接
- **Device Connected!**: 已连接
- **Flashing...**: 刷写中
- **SUCCESS**: 完成

---

## 高级功能

### 批量刷写模式

在配置界面启用"Auto-Flash（连续模式）"：

1. 系统会等待设备连接
2. 检测到设备后自动刷写
3. 完成后等待下一台设备
4. 循环执行

适合批量生产场景。

### 离线固件包

每次成功刷写后，系统会生成一个 `.zip` 包，包含：

- `firmware.hex`: 已注入密钥的固件
- `softdevice.hex`: SoftDevice
- `flash_linux.sh` / `flash_win.bat`: 离线刷写脚本
- `XXX_devices.json`: Find My 配置文件
- （Dynamic 模式）`seed_XXX.hex/bin`: 种子备份

### Session 隔离

Web Studio 使用 Session ID 进行隔离，每次刷写的临时文件存放在独立目录，避免冲突。

---

## 故障诊断

### 常见错误

#### "Debugger Not Found"

- 检查 USB 连接
- 确认驱动已安装（J-Link: nRF Command Line Tools，ST-Link: OpenOCD）

#### "Chip Disconnected"

- 检查 SWD 接线（SWDIO, SWCLK, GND）
- 参考[硬件连接手册](./05-硬件连接手册.md)

#### "Chip Protected"

- 点击"开始刷机"，系统会自动执行 Mass Erase
- 或参考[硬件连接手册 - 芯片保护解除](./05-硬件连接手册.md#芯片保护解除)

#### "Build Failed"

- 检查 SDK 是否正确安装
- 确认 ARM GCC 工具链可用

---

> 相关文档:
>
> - [快速开始](./01-快速开始.md)
> - [命令行刷写工具](./04-命令行刷写工具.md)
